# คู่มือการรันโปรแกรมอย่างละเอียด (Detailed Execution Walkthrough)

คู่มือนี้จัดทำขึ้นเพื่อให้คุณเข้าใจทุกขั้นตอน ตั้งแต่การติดตั้ง การใช้งานคำสั่ง ไปจนถึงการแก้ปัญหาเบื้องต้น

---

## 📋 1. การเตรียมระบบก่อนเริ่มงาน (System Prerequisites)

ก่อนจะรันโปรแกรม คุณจำเป็นต้องมี 3 สิ่งนี้ในเครื่อง:

1.  **Python 3.10+**: แนะนำเป็นเวอร์ชันล่าสุด
2.  **Tesseract OCR**: 
    - ต้องติดตั้งลงใน OS (Windows/Linux)
    - **สำคัญ**: ต้องเลือกติดตั้ง **Lao Data Language** (ภาษาลาว) เพิ่มเติมตอนติดตั้ง Tesseract
3.  **Poppler**: 
    - จำเป็นสำหรับการแปลงไฟล์ PDF เป็นรูปภาพ
    - ต้องตั้งค่า Path ใน Environment Variables ให้ชี้ไปที่โฟลเดอร์ `bin` ของ Poppler

---

## 🛠 2. การติดตั้ง Library (Installation)

เปิด Terminal หรือ Command Prompt ในโฟลเดอร์โปรเจกต์แล้วพิมพ์:

```bash
# ติดตั้งไลบรารีที่จำเป็นทั้งหมด
pip install -r requirements.txt
```

*ไลบรารีหลักประกอบด้วย:*
- `pytesseract`: ตัวเชื่อมต่อ OCR
- `opencv-python`: ตัวจัดการรูปภาพ
- `pdf2image`: ตัวแปลง PDF
- `polars`: ตัวจัดการไฟล์ CSV (ความเร็วสูง)

---

## 🚀 3. เจาะลึกคำสั่งการใช้งาน (Command Line Interface)

รูปแบบคำสั่งพื้นฐานคือ:
`python school_stats_v2.py <คำสั่งไฟล์ PDF> <ชื่อไฟล์ผลลัพธ์> [ทางเลือกเสริม]`

### 3.1 การรันแบบพื้นฐาน (The Basics)
เหมาะสำหรับการใช้งานทั่วไป:
```bash
python school_stats_v2.py "data.pdf" output.csv
```

### 3.2 การเพิ่มความเร็วด้วยคอร์ CPU (High-Performance Mode)
หากไฟล์ PDF มีจำนวนหน้ามาก (เช่น 500+ หน้า) การรันทีละหน้าจะช้ามาก ให้ใช้ `--cores`:
```bash
# รันพร้อมกัน 4 หน้า (ใช้กับ CPU 4 คอร์)
python school_stats_v2.py "data.pdf" output.csv --cores 4
```
*คำแนะนำ: ไม่ควรใส่เกินจำนวนคอร์จริงของคอมพิวเตอร์คุณ*

### 3.3 การสุ่มตรวจหรือเลือกหน้า (Targeted Processing)
คุณไม่จำเป็นต้องรันทั้งไฟล์ทุกครั้ง:
- **เฉพาะหน้า**: `--pages 1,5,10`
- **เป็นช่วง**: `--pages 10-50`
- **ผสม**: `--pages 1-10,15,20-25`
```bash
python school_stats_v2.py "data.pdf" output.csv --pages 13,17
```

### 3.4 โหมดตรวจสอบความถูกต้อง (Deep Debugging)
เมื่อรันด้วย `--debug` โปรแกรมจะทำ 2 อย่างเพิ่ม:
1. พิมพ์ Log การทำงานอย่างละเอียดในหน้าจอ (เช่น พบเส้นกี่เส้น, อ่านหัวข้อเจอไหม)
2. สร้างรูปภาพในโฟลเดอร์ `debug_output` โดยในรูปจะมีการวาด:
   - **วงกลมสีฟ้า/ชมพู**: ตำแหน่งเพศที่ตรวจพบ
   - **เส้นสีเขียว**: เส้นเชื่อมจากเพศไปยังคะแนนที่โปรแกรมอ่านได้
   - **ตัวเลขสีเขียว**: ค่าคะแนนที่ OCR อ่านออกมาได้จริง
```bash
python school_stats_v2.py "data.pdf" output.csv --pages 17 --debug
```

---

## 📈 4. การจัดการผลลัพธ์ (Understanding Output)

เมื่อรันเสร็จ คุณจะได้ไฟล์ `output.csv` ที่มีโครงสร้างดังนี้:
1.  ** ข้อมูลทั่วไป**: รหัสโรงเรียน, ชื่อโรงเรียน, แขวง, ปีการศึกษา
2.  **สรุปภาพรวม**: จำนวนนักเรียนรวม, ชายรวม, หญิงรวม
3.  **แบ่งกลุ่มตามเกณฑ์**: 
    - `>= 45` (เก่งมาก)
    - `35 - 45` (ดี)
    - `25 - 35` (พอใช้)
    - `< 25` (ต้องปรับปรุง)
    - *ทุกกลุ่มจะแยกย่อยเป็น ชาย/หญิง ให้โดยละเอียด*

---

## ❓ 5. การแก้ปัญหาเบื้องต้น (Troubleshooting)

| ปัญหาที่พบ | สาเหตุที่เป็นไปได้ | วิธีแก้ไข |
| :--- | :--- | :--- |
| **Error: Tesseract not found** | ไม่ได้ติดตั้ง Tesseract หรือไม่ได้ตั้ง Path | เช็ค Path ในตัวแปร `pytesseract.tesseract_cmd` ในโค้ด |
| **อ่านคะแนนไม่เจอ (Score=0)** | ตารางจางเกินไป หรือตัวเลขชิดเส้นมาก | ลองรันหน้าเดียวด้วย `--debug` เพื่อดูว่าระบบล็อกเส้นตารางพลาดหรือไม่ |
| **ชื่อโรงเรียนเป็น Unknown** | รูปแบบหัวกระดาษเปลี่ยนไป | ตรวจสอบว่าคำว่า "ມາຈາກ" ใน PDF ถูกต้องหรือไม่ หรือมีรอยเปื้อนบัง |

---
*จัดทำโดย: Antigravity AI Assistant*
